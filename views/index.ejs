<!DOCTYPE html>
<html>
<head>
  <title>Koa MVC Example</title>
</head>
<body>
  <h1>Items</h1>
  <ul>
    <% items.forEach(function(item) { %>
      <li><%= item.name %></li>
    <% }); %>
  </ul>
</body>
</html>
